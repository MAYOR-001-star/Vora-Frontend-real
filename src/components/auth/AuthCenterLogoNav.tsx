import VoraLogo from '../common/VoraLogo';

const AuthCenterLogoNav: React.FC = () => (
  <header className="sticky top-0 z-[100] bg-white border-b border-[#E6E6E6] px-4 sm:px-10 min-h-14 flex items-center justify-center">
    <VoraLogo size="md" />
  </header>
);

export default AuthCenterLogoNav;
